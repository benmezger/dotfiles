#!/usr/bin/bash

. $HOME/.xinitrc
. $HOME/.env-secrets

if [ -f /usr/bin/qtile ]; then
    exec qtile start --config ~/.config/qtile/config.py
elif [ -f /usr/bin/i3 ]; then
    exec i3
elif [ -f /usr/local/bin/dwm ]; then
    . $HOME/.bin/startdwm
fi
